export default function ActivityCard({weekActivity,totalWeek,missed}:{weekActivity:{day:string;submitted:number;total:number}[];totalWeek:number;missed:number}){const max=Math.max(...weekActivity.map(d=>d.submitted),1);return(<div className="bg-surface border border-border rounded-2xl p-5"><div className="flex items-center justify-between mb-4"><div className="text-[14px] font-bold">📈 Активность</div><span className="text-[10px] bg-c-blue/10 text-c-blue rounded-full px-2 py-0.5">7 дней</span></div><div className="text-[11px] text-muted font-mono uppercase mb-2">Отчёты за неделю</div><div className="flex items-end gap-1 h-10 mb-1">{weekActivity.map((d,i)=>(|div key={i} style={{height:`${(d.submitted/max)*100}%`,background:d.submitted===d.total?'#22d3a0':d.submitted>0?'#f59e0b':'#ff5566'}} />))}</div><div className="flex justify-between text-[10px] text-muted font-mono mb-4">{weekActivity.map((d,i)=><span key={i}>{d.day}</span>)}</div><div className="h-px bg-border mb-4" /><div className="grid grid-cols-2 gap-3"><div><div className="text-[10px] text-muted font-mono uppercase">Всего отчётов</div><div className="text-xl font-extrabold text-c-green">{totalWeek}/35</div><div className="text-[11px] text-muted">За з дней</div></div><div><div className="text-[10px] text-muted font-mono uppercase">Пропусков</div><div className="text-xl font-extrabold text-c-red">{missed}</div><div className="text-[11px] text-muted">= {missed*100}₽</div></div></div></div>)}
