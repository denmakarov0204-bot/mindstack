import type {Meeting} from '@/lib/supabase'
export default function MeetingCard({nextMeeting}:{nextMeeting?:Meeting}){if(!nextMeeting)return null;const date=new Date(nextMeeting.date);const daysLeft=Math.ceil((date.getTime()-Date.now())/(1000*60*60*24));return(<div className="rounded-xl p-4 border" style={{background:'linear-gradient(135deg,rgba(124,106,255,0.08),rgba(167,139,250,0.04))',borderColor:'rgba(124,106,255,0.2)'}}><div className="flex justify-between items-start mb-2.5"><div><div className="text-[12px] text-accent2 font-mono">{date.toLocaleDateString('ru-RU',{weekday:'long',day:'numeric',month:'long'})} · 11:00</div><div className="text-lg font-extrabold tracking-tight mt-0.5">Встреча �{nextMeeting.meeting_number}</div></div><span className="text-[10px] bg-accent/15 text-accent2 rounded-full px-2  py-1 font-bold">СКОРП</span></div><div className="text-[12px] text-muted">Каждый участник ~50 мин</div><div className="text-[11px] text-c-green mt-1.5">⏱ через {daysLeft} дней</div></div>)}
